<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.fm.admin.dao.AdminMapper">
    <!--查询超级管理员用于登录-->
    <select id="selectAdmin" resultType="cn.fm.pojo.Admin" parameterType="cn.fm.pojo.Admin">
        select aname ,apwd from admin where aname = #{aname} and apwd = #{apwd};
    </select>

    <insert id="addUser" parameterType="cn.fm.pojo.User">
        insert into user(uname,ucompany,uemail,uupdatetime,code )
        values(#{uname},#{ucompany},#{uemail},#{uupdatetime},#{code});
    </insert>

    <!--查找工作的单位-->
    <select id="selectWorkPlace" resultType="cn.fm.pojo.WorkPlace">
        select wid ,wname from workPlace;
    </select>
    <!--查找用户是不是存在-->
    <select id="selectEmailIfExist" parameterType="java.lang.String" resultType="java.lang.String">
        select uemail from user where uemail = #{uemail};
    </select>
</mapper>